// owner: web-auth-rbac — — Page not found.
// Design: web/roles and screens/sheets, modals, drawers, menus/Sign in — split brand panel with SSO.jpg
// Placeholder created by web-architect (Phase 1a): route + lazy boundary only, no screen content.
import { PagePlaceholder } from '@/shared/ui/PagePlaceholder';

export default function NotFoundPage() {
  return <PagePlaceholder screen="—" title="Page not found" />;
}
